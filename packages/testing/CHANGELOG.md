# Changelog

## 0.1.0-alpha.1 (2026-09-13)

Add the `mp-testing` CLI, automatic npm installation of a staged pre-commit policy, existing-hook chaining, persistent project setup, and explicit CI checks. Reject recognizable Playwright and promise timer sleeps in test sources. The parser requires Node ^22.18.0 or >=24.11.0. Packed-install contracts exercise actual rejected commits, partial staging, hook preservation, ignored scripts, fresh clones and nested projects.

## 0.1.0-alpha.0 (2026-09-12)

Initial experimental extraction, published locally as the registry bootstrap without GitHub provenance. Zero consumer adopters; no stable compatibility promise. See the repository source inventory and verification report.
